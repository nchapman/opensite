<h1>Editing user</h1>

<% form_for(@user) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to "Show", @user %> |
<%= link_to "Back", users_path %>