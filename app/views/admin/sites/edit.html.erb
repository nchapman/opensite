<% @page_title = "Editing site" %>

<% form_for([:admin, setup_site(@site)]) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to "Show", [:admin, @site] %> |
<%= link_to "Back", admin_sites_path %>