<% @page_title = "New style sheet" %>

<% form_for([:admin, @site, @style_sheet]) do |f| %>
  <%= render :partial => "admin/textual_assets/form", :locals => { :f => f } %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to "Back", admin_site_style_sheets_path(@site) %>