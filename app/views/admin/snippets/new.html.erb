<h1>New snippet</h1>

<% form_for([:admin, @site, @snippet]) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to "Back", admin_site_snippets_path(@site) %>
