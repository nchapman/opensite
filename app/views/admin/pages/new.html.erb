<% @page_title = "New page" %>

<% form_for([:admin, @site, setup_page(@page)]) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to "Back", admin_site_pages_path(@site, @pages) %>
