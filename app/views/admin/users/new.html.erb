<% @page_title = admin? ? "New user" : "Register" %>

<% form_for([:admin, @user]) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= f.submit admin? ? "Create" : "Register" %>
  </p>
<% end %>

<%= link_to "Back", admin_users_path if admin? %>